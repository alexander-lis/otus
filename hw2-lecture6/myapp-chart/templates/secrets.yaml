apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
stringData:
  ConnectionString: 'server=myapp-db-service;port=3306;user=root;password=password;'
  DbPassword: 'password'